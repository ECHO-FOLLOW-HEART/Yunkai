backendKeys: {
  production: {
    mongo = mongo
    redis-main = redis
    rabbitmq = rabbitmq
  }
  dev: {
    mongo-dev = mongo
    redis-main = redis
    rabbitmq = rabbitmq
  }
  test: {
    mongo-dev = mongo
    redis-main = redis
    rabbitmq = rabbitmq
  }
}

confKeys: {
  production: {
    yunkai = yunkai
  }
  dev: {
    yunkai-dev = yunkai
  }
  test: {
    yunkai-test = yunkai
  }
}
